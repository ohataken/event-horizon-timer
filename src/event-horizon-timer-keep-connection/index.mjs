/**
 * event-horizon-timer-keep-connection
 * @param {Object} event event
 * @returns {Object} response
 */
export async function handler(event) { // eslint-disable-line no-unused-vars -- event
  return {
    statusCode: 200,
    body: "OK",
  };
}
