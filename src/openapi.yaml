openapi: 3.0.4
info:
  version: 1.0.12
  title: Event Horizon Timer
tags:
  - name: timer
    description: timer
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: hex
  schemas:
    Error:
      type: object
      properties:
        message:
          description: error message
          type: string
paths:
  /api/timers:
    post:
      summary: create a new timer
      description: |-
        create a new timer
      tags:
        - timer
      responses:
        '201':
          description: success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  timer:
                    description: timer
                    type: object
                    properties:
                      id:
                        description: id
                        type: string
                        format: uuid
                        example: 3f4d7fca-4a06-4af7-8da4-90445e52de17
                      token:
                        description: token
                        type: string
                        format: hex
                        example: "112c414625408e15974642192ca630ba854880ff761053cdd0df6483dcd531cf"
                      status:
                        description: status
                        type: string
                        example: stopped
                      target_time:
                        description: target time
                        type: number
                        example: 1757817633
                      duration:
                        description: duration
                        type: integer
                        example: 0
        '500':
          description: server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/timers/{id}:
    get:
      summary: get a timer
      description: |-
        get a timer
      tags:
        - timer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  timer:
                    description: timer
                    type: object
                    properties:
                      id:
                        description: id
                        type: string
                        format: uuid
                        example: 3f4d7fca-4a06-4af7-8da4-90445e52de17
                      status:
                        description: status
                        type: string
                        enum: [running, stopped]
                      target_time:
                        description: target time
                        type: number
                        example: 1757817633
                      duration:
                        description: duration
                        type: integer
                        example: 180
        '404':
          description: timer not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      summary: update a timer
      description: |-
        update a timer
      tags:
        - timer
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - token
                - status
                - target_time
                - duration
              properties:
                status:
                  description: timer status
                  type: string
                  enum: [running, stopped]
                  example: "running"
                target_time:
                  description: target time
                  type: number
                  minimum: 0
                  example: 1757817633
                duration:
                  description: duration
                  type: integer
                  minimum: 0
                  example: 180
      responses:
        '200':
          description: success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  timer:
                    description: timer
                    type: object
                    properties:
                      id:
                        description: id
                        type: string
                        format: uuid
                        example: 3f4d7fca-4a06-4af7-8da4-90445e52de17
                      status:
                        description: status
                        type: string
                        enum: [running, stopped]
                      target_time:
                        description: target time
                        type: number
                        example: 1757817633
                      duration:
                        description: duration
                        type: integer
                        example: 180
        '401':
          description: authorization header is missing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: invalid token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '400':
          description: no valid attributes to update.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: timer not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/timers/{id}/start:
    post:
      summary: start a timer
      description: |-
        start a timer
      tags:
        - timer
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  timer:
                    description: timer
                    type: object
                    properties:
                      id:
                        description: id
                        type: string
                        format: uuid
                        example: 3f4d7fca-4a06-4af7-8da4-90445e52de17
                      status:
                        description: status
                        type: string
                        enum: [running, stopped]
                      target_time:
                        description: target time
                        type: number
                        example: 1757817633
                      duration:
                        description: duration
                        type: integer
                        example: 180
        '401':
          description: authorization header is missing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: invalid token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: timer not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/timers/{id}/stop:
    post:
      summary: stop a timer
      description: |-
        stop a timer
      tags:
        - timer
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  timer:
                    description: timer
                    type: object
                    properties:
                      id:
                        description: id
                        type: string
                        format: uuid
                        example: 3f4d7fca-4a06-4af7-8da4-90445e52de17
                      status:
                        description: status
                        type: string
                        enum: [running, stopped]
                      target_time:
                        description: target time
                        type: number
                        example: 1757817633
                      duration:
                        description: duration
                        type: integer
                        example: 180
        '401':
          description: authorization header is missing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: invalid token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: timer not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
