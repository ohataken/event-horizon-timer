import { defineConfig } from "eslint/config"; // eslint-disable-line n/no-unpublished-import -- eslint/config
import eslintConfigESLint from "eslint-config-eslint"; // eslint-disable-line n/no-unpublished-import -- eslint-config-eslint

export default defineConfig([eslintConfigESLint]);
